spring.application.name=OPombo

# Configurações do banco de dados
spring.datasource.url=jdbc:mysql://localhost:3306/opombo?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=88090060

# Driver do MySQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA (Opcional, caso esteja usando JPA)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

server.servlet.context-path=/opombo
